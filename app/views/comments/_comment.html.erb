<p>
  <strong>Commenter:</strong>
  <%= comment.commenter %>
</p>

<p>
  <strong>Comment:</strong>
  <%= comment.body %>
  <li><%= link_to "Edit", edit_article_comment_path(@article, comment) %></li>
  <li><%= link_to "Delete", [comment.article, comment], data: {
                  turbo_method: :delete,
                  turbo_confirm: "Are you sure?"
                } %></li>
</p>

<%
# if edit_article_comment_path(@article, @comment), error id = nil
# if edit_article_comment_path(@comment), error article_id = nil 
%>